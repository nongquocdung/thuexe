import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  rootAppTreeView: {
    height: "80vh",
    flexGrow: 1,
    height: "auto",
    overflow: "hidden",

    // maxwidth: 300,
  },
});
